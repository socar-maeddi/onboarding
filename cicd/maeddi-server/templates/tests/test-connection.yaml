apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maeddi-server.fullname" . }}-test-connection"
  labels:
    {{- include "maeddi-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maeddi-server.fullname" . }}:{{ .Values.fastapi.service.port }}']
  restartPolicy: Never
